name: Testkit CI

on:
  pull_request:
    branches: [main]
    paths:
      - 'testkit/**'
      - 'extensions/**'
      - 'VERSION'

jobs:
  build:
    if: github.repository == 'tracedown/lacelang' && !startsWith(github.event.pull_request.title, 'WIP:')
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
    steps:
      - uses: actions/checkout@v4

      - name: Install OpenSSL dev (Linux)
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y libssl-dev

      - name: Install OpenSSL (macOS)
        if: runner.os == 'macOS'
        run: brew install openssl@3

      - name: Set OpenSSL paths (macOS)
        if: runner.os == 'macOS'
        run: |
          echo "CFLAGS_SSL=-I$(brew --prefix openssl@3)/include" >> "$GITHUB_ENV"
          echo "LDFLAGS_SSL=-L$(brew --prefix openssl@3)/lib" >> "$GITHUB_ENV"

      - name: Build
        run: |
          cd testkit/src && make clean && make EMBED_VECTORS=1 \
            CFLAGS="-std=c99 -Wall -Wextra -Wpedantic -O2 -D_POSIX_C_SOURCE=200809L -D_GNU_SOURCE -Ithird_party/cjson -DLACE_CONFORMANCE_VERSION='\"$(cat ../../VERSION)\"' -DWITH_TLS -Wno-overlength-strings $CFLAGS_SSL" \
            LDFLAGS="$LDFLAGS_SSL -lpthread -lssl -lcrypto"

      - name: Smoke test
        run: cd testkit/src && make test
