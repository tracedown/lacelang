{
  "id": "count_wrong_arity",
  "description": "count() with no arguments inside .assert() is FUNC_ARG_TYPE — it requires exactly one (spec §8.1).",
  "spec_refs": [
    "lace-spec.md §8.1"
  ],
  "type": "validate",
  "input": {
    "source": "get(\"$u\").assert({ expect: [count() eq 1] })\n",
    "variables": [
      "u"
    ],
    "context": {
      "maxRedirects": 10,
      "maxTimeoutMs": 300000
    }
  },
  "expected": {
    "errors": [
      {
        "code": "FUNC_ARG_TYPE",
        "callIndex": 0,
        "chainMethod": "assert"
      }
    ],
    "warnings": []
  }
}
