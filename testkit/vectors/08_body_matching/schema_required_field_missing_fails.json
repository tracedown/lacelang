{
  "id": "schema_required_field_missing_fails",
  "description": "body: schema($schemaVar) fails when a required field is absent from the response body (spec §15.8, §4.5).",
  "spec_refs": [
    "lace-spec.md §15.8",
    "lace-spec.md §4.5"
  ],
  "type": "execute",
  "input": {
    "source": "get(\"http://127.0.0.1:{port}/x\").expect(status: 200, body: schema($schemaVar))\n",
    "variables": {
      "schemaVar": {
        "type": "object",
        "properties": {
          "id": { "type": "integer" },
          "name": { "type": "string" }
        },
        "required": ["id", "name"]
      }
    },
    "http_mock": [
      {
        "callIndex": 0,
        "outcome": "response",
        "status": 200,
        "headers": { "content-type": "application/json" },
        "body": "{\"id\": 1}"
      }
    ]
  },
  "expected": {
    "result": {
      "outcome": "failure",
      "runVars": {},
      "calls": [
        {
          "index": 0,
          "outcome": "failure",
          "assertions": [
            {
              "method": "expect",
              "scope": "status",
              "op": "eq",
              "outcome": "passed",
              "actual": 200,
              "expected": 200,
              "options": null
            },
            {
              "method": "expect",
              "scope": "body",
              "op": "eq",
              "outcome": "failed",
              "actual": "IGNORED",
              "expected": "IGNORED",
              "options": null
            }
          ]
        }
      ],
      "actions": {}
    },
    "ignore": [
      "calls[*].config",
      "calls[*].request",
      "calls[*].response",
      "calls[*].redirects",
      "calls[*].warnings",
      "calls[*].error"
    ]
  }
}
